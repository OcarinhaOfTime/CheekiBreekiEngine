#pragma once
#include <GL\glew.h>


namespace CheekiBreekiEngine {
	class Renderer {
	public:
		void start();	// must creates it's context, 
		void update();
		void terminate();
		// start
		// send data
		// update elements
		// clean data
	};
}